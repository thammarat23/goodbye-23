# goodbye-23 — ระบบสแกนหนังสือกำลังภายใน

## ชื่อเรียกเครื่อง (สำคัญ — ผู้ใช้เปิดสองเครื่องพร้อมกัน)

| ชื่อเรียก | หมายถึง | หน้าที่ |
|---|---|---|
| **เครื่องหลัก** | คอมประจำของผู้ใช้ (Windows) | งานทั่วไป ผู้ใช้คุมเอง |
| **เครื่องที่สอง** | เครื่องคลาวด์ที่ Claude Code รันอยู่ (Ubuntu 24.04) | **เน้นงานสแกนเอกสาร/OCR เท่านั้น** |

เมื่อผู้ใช้พูดว่า "เครื่องที่สอง" หมายถึงเครื่องนี้ ไม่ต้องถามซ้ำ
งานที่ขอในเครื่องที่สองให้ถือว่าเป็นงานสายสแกน/OCR เป็นค่าเริ่มต้น

## สเปกเครื่องที่สอง

- RAM 15 GB · CPU 4 คอร์ · ดิสก์เขียนได้ราว 30 GB
- Ubuntu 24.04 · Python 3.11
- เป็นคอนเทนเนอร์ชั่วคราว **ไฟล์ที่ไม่ commit หรือไม่อัปขึ้นไดรฟ์จะหายเมื่อจบเซสชัน**

## การส่งงาน

ส่งงานขึ้น **Google Drive** เสมอ ไม่เก็บไว้แค่ในเครื่องที่สอง
ดูปลายทางและขั้นตอนใน `scan/README.md` หัวข้อ "ส่งงานขึ้นไดรฟ์"

## โครงสร้าง

```
scan/
  scan_books.py   ไปป์ไลน์หลัก (batch / watch / --pdf / --check)
  thai_clean.py   ทำความสะอาดข้อความ OCR ภาษาไทย
  deliver.py      รวมผลงานเป็นชุดพร้อมอัปขึ้นไดรฟ์
  setup.sh        ติดตั้งเครื่องมือทั้งหมดบนเครื่องเปล่า
work/             โฟลเดอร์ทำงานตอนรัน (ไม่ commit)
```

## ข้อควรระวัง

- `work/` ถูก gitignore ไว้ ผลงานจริงต้องอัปขึ้นไดรฟ์
- ชื่อไฟล์ภาษาไทยต้องไม่ถูกแปลงเป็น `_` — สระและวรรณยุกต์ไทยเป็น combining
  mark ที่ `str.isalnum()` คืน False ดู `safe_name()` ใน `scan_books.py`
- ตัวคลีนต้องไม่กินเครื่องหมายคำพูด `"` `'` เพราะบทสนทนาในนิยายใช้ตลอด
- ระบบเดิมบนไดรฟ์ (`scan_books.py` เวอร์ชัน Windows) ฮาร์ดโค้ดพาธ
  `C:\Program Files\Tesseract-OCR` ไว้ ใช้บนเครื่องที่สองไม่ได้
  เวอร์ชันในรีโปนี้ตรวจหาที่ตั้งเองและใช้ชื่อโฟลเดอร์เดียวกันจึงเข้ากันได้
