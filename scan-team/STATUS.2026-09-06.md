# 📊 STATUS — scan-team (ตรวจคืบหน้า 6 ก.ย. 2569 · 21:15 น.)

> ตรวจจากคอมเครื่อง 2 (คลาวด์) ผ่าน Google Drive · ไม่ได้แตะ pipeline

## สรุป: สเตจ "แก้คำผิด" วิ่งอยู่ · สเตจ OCR เงียบมา 7 วัน

| สเตจ | ที่อยู่ | สัญญาณล่าสุด | สถานะ |
|---|---|---|---|
| แก้คำผิด/tidy | `_ocr/_fixed/` | `_queue.log` + `1. นางสิงห์มือปราบ เล่ม 1.json` → 6 ก.ย. 21:14 | ✅ วิ่งอยู่ (ไม่กี่นาทีก่อน) |
| ยาม (watch daemon) | `scan-team/_watch.log` | 6 ก.ย. 21:15 · ไฟล์โตจาก 1.76 → 2.09 MB | ✅ วิ่งอยู่ |
| OCR (scan_parallel workers) | `scan-team/_worker1-2.log`, `_scan_queue.json` | 30 ส.ค. 18:55 | 🔴 ไม่ขยับ 7 วัน |
| ผลลัพธ์ OCR | `_ocr/*.txt` · `*.ckpt.json` | 30 ส.ค. 18:55 | 🔴 ไม่มีเล่มใหม่ 7 วัน |

## ⚠️ ยกเลิกข้อสันนิษฐานเดิมเรื่อง "Drive ซิงก์ค้าง"

บันทึก 2 ก.ย. เดาว่าไฟล์ค้างเพราะ log ใหญ่ 25-28 MB ไปขวางคิวอัปโหลด — **ไม่ใช่**
วันนี้บัญชีเดียวกันซิงก์ไฟล์ขึ้นเป็นสิบไฟล์ (หลายโปรเจกต์ รวมทั้ง `_ocr/_fixed/`) ตอน 21:13-21:15 น.
แปลว่าตัวซิงก์ปกติดี → ไฟล์ฝั่ง OCR ที่ไม่ขยับ **คือไม่มีอะไรถูกเขียนจริง ๆ**

## 🔎 จุดที่ต้องเช็คที่เครื่องบ้าน (ตัดสินได้ด้วยข้อเดียว)

ดู **เวลาแก้ไขไฟล์ในเครื่อง** ของ `G:\...\scan-team\_worker1.log`

- ถ้าเวลาในเครื่อง **สดวันนี้** → เป็นปัญหาซิงก์เฉพาะไฟล์นั้น (แก้ด้วย log rotate)
- ถ้าเวลาในเครื่อง **ค้างที่ 30 ส.ค.** → worker OCR ไม่ทำงานจริง (ค้าง/ถูกปิด) แม้โปรเซสจะยังอยู่

## ตัวเลขล่าสุดที่มี (ยังเป็นของ 30 ส.ค. 18:48 — ไม่มีอันใหม่กว่านี้)

| ประเภท | done | pending | error |
|---|---|---|---|
| การ์ตูน | 2,042 | 0 | 3 |
| นิยาย | 1,511 | 1,009 | 134 |

`_ocr/_fixed/_queue_status.json` (เขียนล่าสุด 29 ส.ค.): `total 1526 · index 6 · current "1 ดาบมรกต BW"`
— ไฟล์สถานะตัวนี้ก็ไม่ถูกอัปเดตแล้ว ทั้งที่คิวยังวิ่ง จึงใช้ดูความคืบหน้าไม่ได้

## 📌 งานค้าง

1. 🔴 เช็ค `_worker1.log` ตามข้อ 🔎 ข้างบน แล้วค่อยตัดสินใจเรื่อง OCR
2. 🔴 `comics.skill-999.com` พัง — เช็ค tunnel/cloudflared + server 5501
3. 🟡 ทำ `_status_snapshot.txt` (ไฟล์เล็ก เขียนทับเป็นระยะ) ให้เครื่องอื่นเห็นตัวเลขสด
4. 🟡 นิยาย error 134 เล่ม ยังไม่ได้ไล่สาเหตุ
